[project]
name = "spy-benchmarks"
version = "0.1.0"
description = "SPy benchmarks"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=2.5.0",
    "spylang",
    "pytest",
]


[tool.uv]
package = false

[tool.uv.sources]
spylang = { path = "../spy", editable = true }
